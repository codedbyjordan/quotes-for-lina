const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      fontFamily: {
        "hurricane": ["Hurricane", "cursive"]
      }
    },
  },

  plugins: [],
};

module.exports = config;
